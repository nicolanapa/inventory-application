<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Genre <%= genre.id %></title>

        <link rel="stylesheet" href="../../styles/style.css" />
        <link rel="stylesheet" href="../../styles/form.css" />
    </head>
    <body>
        <%- include("../partials/header") %>

        <main>
            <h2><%= genre.game_genre %></h2>

            <h3>Found in these Games</h3>
            <ul>
                <% for (let i = 0; i < games.length; i++) { %>
                    <li><a href="/game/<%= games[i].game_id %>"><%= games[i].game_name%></a></li>
                <% } %>
            </ul>

            <%- include("../partials/deleteForm", { path: "genre", id: genre.id, name: genre.game_genre }) %>
            
            <!-- Genre not found Handling -->
        </main>
    </body>
</html>
